return {
  "neovim/nvim-lspconfig",

  -- config = function()
  --   local lspconfig = require("lspconfig")

  -- lspconfig.cobol_ls.setup({})
  -- lspconfig.configs.cobol_ls = {
  --   default_config = {
  --     cmd = { "cobc" },
  --     filetypes = { "cobol" },
  --     root_dir = lspconfig.util.root_pattern("cobol.cbl"),
  --   },
  -- }
  -- end,
}
